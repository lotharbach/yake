apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gardener-extensions
  namespace: yake
spec:
  install:
    remediation:
      retries: 3
    createNamespace: true
  interval: 1m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: gardener-extensions/chart
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: yake
        namespace: yake
      interval: 1m
  valuesFrom:
    - kind: Secret
      name: yake-config
      optional: false
    - kind: Secret
      name: gardener-extension-values
      optional: true
