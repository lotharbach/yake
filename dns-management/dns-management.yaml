apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dns-management
  namespace: yake
spec:
  install:
    remediation:
      retries: 3
    createNamespace: true
  interval: 1m
  targetNamespace: garden
  dependsOn:
    - name: dns-management-config
  chart:
    spec:
      chart: external-dns-management
      version: 0.18.6
      sourceRef:
        kind: HelmRepository
        name: gardener-community-charts
        namespace: yake
      interval: 1m
  valuesFrom:
    - kind: Secret
      name: dns-management-yake-values
      optional: false
    - kind: Secret
      name: dns-management-values
      optional: true
